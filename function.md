1. 請增加功能

記帳 在記帳內的下方增加一個按鈕，

按下這個按鈕後，下拉一個介面

這個介面支持一筆帳單中不同的人物使用

1個付款人，多個使用人，但使用的金額大家都不一樣的。

1. 選擇付款人給用戶下拉選單(顯示原本已有的付錢人(單選) 中的人物)。
2. 選擇使用人給用戶下拉選單(顯示原本已有的付錢人(單選) 中的人物)。
3. 增加按鈕，用戶可以增加多筆數
4. 自動校對金額，所有的金額總和必須和總金額一致，在加上方建立誤差金額顯示(正常時誤差金額為0)
5. 提交按鈕，用戶按下這個按鈕後，以上的資料調用 getAll()function , 一筆一筆用迴圈提交到後台, 每個使用人只有一筆帳(合併計算，(如下方例子 KEN , 2和3筆都有他，所以金額是10+100+90/3 = 140)，並在備注中，顯示合併金額和明細的過程

例如 :

總金額 200  備注: 用戶填_QWERT(後面字母隨機5位英文字大寫)

1. 選擇付款人 ben  金額50  內容: X + 備注 選擇使用人Vincent 
2. 選擇付款人 ben  金額100  內容: X + 備注 選擇使用人Ken
3. 選擇付款人 ben  金額10  內容: X + 備注 選擇使用人Ken
4. 選擇付款人 ben  其他費用 90  內容: X + 備注 選擇使用人Vincent,Ken,Ben

增加筆數按鈕
提交按鈕






